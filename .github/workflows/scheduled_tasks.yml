name: Calorista Daily Tasks

on:
  schedule:
    - cron: '0 1 * * *'  # Simplified to run daily at 1 AM UTC (3 AM Kyiv)
  workflow_dispatch:

jobs:
  daily-tasks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'
          
      - name: Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          pip install -r calorista/requirements.txt
          
      - name: Setup environment
        run: |
          echo "🕒 Timezone: $(date)"
          echo "🐍 Python version: $(python --version)"
          echo "📁 Working directory: $(pwd)"
          echo "::notice::Environment setup complete"
          
      - name: Run main script
        run: |
          echo "🚀 Starting main.py..."
          python calorista/main.py 2>&1 | tee -a main.log
          echo "::group::Main script output"
          cat main.log
          echo "::endgroup::"
        env:
          REDIS_URL: ${{ secrets.REDIS_URL }}
          TZ: 'Europe/Kiev'
          
      - name: Run historical loader
        run: |
          echo "🔄 Starting historical loader..."
          python calorista/load_historical_to_redis.py 2>&1 | tee -a loader.log
          echo "::group::Loader output"
          cat loader.log
          echo "::endgroup::"
        env:
          REDIS_URL: ${{ secrets.REDIS_URL }}
          
      - name: Archive logs
        if: always()
        uses: actions/upload-artifact@v4  # Updated to v4
        with:
          name: execution-logs
          path: |
            main.log
            loader.log
          
      - name: Completion status
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "::notice title=Success::✅ All tasks completed successfully!"
          else
            echo "::error title=Failed::❌ Some tasks failed"
          fi
          echo "📊 View run details: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
